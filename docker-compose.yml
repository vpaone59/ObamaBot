version: '3'

services:
  bot:
    build: .
    container_name: python3-obamabot
    volumes:
      - C:\Users\vpaon\Documents\logs\ObamaBot\bot.log:/app/bot.log
      - C:\Users\vpaon\Documents\DB\obama.db:/app/database/obama.db
    environment:
      - DB_PATH=/app/database/obama.db
    command: [ "python3", "main.py" ]
    restart: unless-stopped
